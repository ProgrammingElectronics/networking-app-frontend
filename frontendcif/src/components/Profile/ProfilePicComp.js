import React from 'react'

//styles
import "./ProfilePicStyles.css"

const ProfilePicComp = () => {
    //will include logic on grabbing signed-in user's name so can be displayed

    return (
    
        <div className="profileImgContainer">
            {/* insert profile img here */}
        </div>
     
    )
}

export default ProfilePicComp
